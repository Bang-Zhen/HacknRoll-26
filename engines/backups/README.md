Backup (non-executed) reference engines.

These samples are intentionally not wired into the runtime and do not affect
functionality. They exist as niche-language backups / reference implementations
for future swaps.
